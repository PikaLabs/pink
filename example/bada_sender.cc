#include "bada_sender.h"

void BadaSender::PeriodMain()
{
  // ping_.set_t(pink::Ping::PING);
  // ping_.set_address("127.0.0.1");
  // ping_.set_port(9223);

  // char *pkt = new char [1024];
  // int siz = ping.ByteSize()+4;
  // ping.SerializeToArray(pkt + 4, siz);

  // uint32_t code_len = ping.ByteSize();
  // uint32_t u;

  // u = htonl(code_len);
  // memcpy(pkt, &u, sizeof(uint32_t));

  // int host_port= 9211;
  // char* host_name="127.0.0.1";
}
